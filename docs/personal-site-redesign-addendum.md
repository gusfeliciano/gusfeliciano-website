# gusfeliciano.com Redesign — Strategy Addendum

Paste this into your EXISTING "gusfeliciano.com Redesign" Claude Design project as a follow-up
message (after you've picked a direction). It folds the new ecosystem strategy into the redesign
so the personal site and madeordinary.com stay coherent.

---

## The addendum — paste this block

> Since the original brief, I've locked a content strategy for how this site fits with my indie
> studio (Made Ordinary, madeordinary.com — being designed separately). Fold these changes into
> the direction we're developing:
>
> ## 1. Identity line (hero + about)
> The 2026 identity is: **Senior Technical Program Manager / AWS Solutions Architect who ships —
> author of basecamp (open-source memory bank for AI coding agents) and founder of Made Ordinary,
> an indie app studio.** Design the hero so this dual identity reads in one composed glance.
> Note: until the studio's public announcement, the copy stays soft ("currently building Made
> Ordinary, an indie app studio") with no outbound link — design both the pre-launch and
> post-launch states of that line.
>
> ## 2. Projects page restructure
> - **Flagship tier:** basecamp gets a case-study-style entry — what it is, why it exists, links
>   to the GitHub repo AND its announcement post on this blog
> - **Studio tier:** a Made Ordinary entry that, post-launch, links OUT to madeordinary.com
>   (pre-launch: the soft no-link state). The studio's apps are NOT detailed here — one card,
>   pointing at their real home
> - **Archive tier:** Cloud Resume Challenge entries and this website itself, compact list, no
>   big cards
>
> ## 3. Footer (sitewide)
> Links: madeordinary.com ("my studio"), basecamp repo, RSS icon (feed is coming — design the
> affordance now), GitHub, LinkedIn. Auto-year copyright.
>
> ## 4. NEW deliverable: OG / social card templates (1200×630)
> Design two:
> - **Default site card** — used for home/about/projects when shared
> - **Post card** — derived from a post's cover image + title overlay, must work with the
>   existing wide cover images
> These ship before the redesign itself (they're launch infrastructure), so keep them simple
> enough to build standalone — typographic, no complex layout dependencies.
>
> ## 5. Blog stays the spine
> The strategy confirmed: ALL my dated writing lives here — cloud/TPM posts, basecamp/AI-tooling
> posts, AND the Made Ordinary making-of stories. The studio site hosts no blog. So the blog
> index and post pages carry three content pillars; make sure the design doesn't visually assume
> a single topic.
>
> ## 6. Confirmed non-features (don't design these)
> No /now page, no newsletter signup, no tags/categories UI (until ~12+ posts), no comments, no
> search, no resume download button for now (it returns only with a fresh PDF in a future
> recruiting-focused refresh — but keep a sensible slot for it in the hero/identity area).
>
> ## 7. Relationship to the Made Ordinary design
> Distinct brands, founder-level kinship: they should never be mistaken for each other (this site
> is editorial/technical; the studio is warm/paper-like), but they shouldn't clash when a reader
> clicks between them. Avoid using the studio's exact accent color as this site's accent.
